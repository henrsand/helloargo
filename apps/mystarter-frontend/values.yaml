name: mystarter-frontend

image:
  repository: henrsand/mystarter-frontend
  tag: "822086a689172da3c7696a44f70eee0655dc2180"

replicas: 2

containerPort: 3000

service:
  port: 80
  targetPort: 3000

resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "256Mi"
    cpu: "200m"

probes:
  liveness:
    path: /
    port: 3000
    initialDelaySeconds: 20
    periodSeconds: 5
  readiness:
    path: /
    port: 3000
    initialDelaySeconds: 15
    periodSeconds: 5

configMap:
  data:
    VITE_ENVIRONMENT: "Argo"