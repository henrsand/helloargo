apiVersion: v1
kind: Service
metadata:
  name: hellok8s
spec:
  selector:
    app: hellok8s
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: NodePort  # Change to LoadBalancer if you want external access