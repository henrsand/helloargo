apiVersion: v1
kind: Service
metadata:
  name: fibapi
spec:
  selector:
    app: fibapi
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: NodePort  # Change to LoadBalancer if you want external access