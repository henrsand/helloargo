apiVersion: v1
kind: Service
metadata:
  name: fibapi
spec:
  selector:
    app: fibapi
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: NodePort  # Change to LoadBalancer if you want external access